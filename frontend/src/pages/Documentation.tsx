import { BookOpen } from "lucide-react";

const steps = [
  ["Загрузить нагрузку", "Откройте «Нагрузка», выберите CSV-файл с интервальными данными и запустите импорт."],
  ["Рассчитать потребность", "Откройте «Потребность», задайте период и выполните расчёт по загруженной нагрузке."],
  ["Сформировать график", "Откройте «Графики», выберите период и создайте черновик графика."],
  ["Подтвердить график", "Проверьте назначения и подтвердите период после согласования с ответственным пользователем."],
  ["Опубликовать график", "Опубликуйте подтверждённый период, чтобы он стал рабочей версией расписания."],
  ["Импортировать факт", "Откройте «Отчёты» и загрузите CSV-файл фактической работы операторов."],
  ["Посмотреть план/факт", "В «Отчётах» выберите период и сравните плановые назначения с фактической работой."],
  ["Экспортировать CSV", "Используйте кнопки экспорта в графиках, покрытии и отчётах план/факт."]
];

export function Documentation() {
  return (
    <section className="panel">
      <div className="section-title">
        <div>
          <h2>Документация</h2>
          <p>Краткие рабочие инструкции для основных WFM-сценариев.</p>
        </div>
        <BookOpen size={28} />
      </div>
      <div className="doc-steps">
        {steps.map(([title, text], index) => (
          <article key={title}>
            <span>{index + 1}</span>
            <div>
              <h3>{title}</h3>
              <p>{text}</p>
            </div>
          </article>
        ))}
      </div>
    </section>
  );
}
